作业一：

请使用延时定时器模拟轮循定时器，整道题不允许使用数字类型，让一个变量的值从0，每一秒加1。



作业二：

请口算出结果

1+‘2’*3    = ？ 7

1*2 -‘3’   = ？ -1

‘1’+2 *‘3’ = ？ '16'



作业三：

根据内容写上相应的js。

请写出全局变量a的值为10。

当前环境下声明变量b的值为a。

让a变成假值。

写一个函数c，他返回一个真值。

打印函数c的返回值。